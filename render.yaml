services:
  - type: web
    name: virtual-bingo
    runtime: node
    plan: free
    branch: main
    buildCommand: echo "No build needed - zero dependencies"
    startCommand: node server.js
    healthCheckPath: /
    autoDeploy: true
